name: example
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^0.5.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  easy_firebase_auth:
    path: ../

flutter:
  uses-material-design: true